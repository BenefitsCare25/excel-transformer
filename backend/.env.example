# Google Maps API Configuration
# Get your API key from: https://developers.google.com/maps/documentation/geocoding/get-api-key
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Postal Code Master File Configuration
# Optional: Custom path to postal code master file (if not using default locations)
# POSTAL_CODE_MASTER_FILE=path/to/your/postal_code_master.xlsx
# POSTAL_CODE_FALLBACK_PATH=path/to/fallback/postal_code_master.xlsx

# Directory Configuration
# Optional: Custom upload and processed file directories
# UPLOAD_FOLDER=uploads
# PROCESSED_FOLDER=processed

# Optional: Set to production for deployment
FLASK_ENV=development

# Optional: Custom port (defaults to 5000)
PORT=5000